Great news, <%= @interest.artist.name %> have released some new tunes:

<%= @new_tracks.each do |track| %>
			<%= track.name %> - <%= raw track.spotify_link %>
<% end %>

Happy tracking!

The Qusic Team