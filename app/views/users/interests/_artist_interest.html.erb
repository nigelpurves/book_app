<tr>
  <td class="name"><%= artist_interest.name.titlecase %></td>
  <td class="time"><%= time_ago_in_words(artist_interest.created_at) %></td>
	<td class="coolnessranking">
    <% if artist_interest.is_last_interest? %>
      You're the <%= artist_interest.position.ordinalize %> tracker of this artist!
    <% else %>
      You were the <%= artist_interest.position.ordinalize %>
      of <%= artist_interest.track.interests.count %> trackers!
    <% end %>
  </td>
  <td class="actions">
    <% if current_user?(artist_interest.user) %>
      <%= link_to "delete", user_interest_path(artist_interest.user, artist_interest), method: :delete, data: {confirm: "Are you sure?"} %>
    <% end %>
  </td>
</tr>
